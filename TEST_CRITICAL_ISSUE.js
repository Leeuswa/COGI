// 테스트용 파일 — 이슈 승인 흐름(CRITICAL) 검증 목적, 검증 후 PR 닫고 브랜치 삭제 예정
function connectToPaymentApi() {
  const apiKey = "sk-test-FAKE1234567890ABCDEFGHIJKLMNOPQRSTUVWX";
  const password = "hardcoded-test-password-123";

  const response = eval(userInput);

  return response;
}

module.exports = { connectToPaymentApi };
